package com.dakoto.datastructuresandalgorithms.algorithms.sort;

import java.util.Comparator;

public class ComparatorPrimer implements Comparator<Object>{

    @Override
    public int compare(Object local, Object o2) {
/**
 * same os compareTo, where first argument is the local and second argument is the foriegner
 *
 * therefore if local is greater than foreigner, return -1;
 *           if local is equal to foreigner, return 0;
 *           if local is less than foreigner, return 1
 *
 *           compareTo basically asks the question, is foreigner greater than or equal to local? if yes, return a positive value
 */
        return 0;
    }
}
